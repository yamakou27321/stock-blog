---
import BaseHead from '../../components/BaseHead.astro';  // ここは「../../」が正解
import Header from '../../components/Header.astro';    // ここも「../../」
import Footer from '../../components/Footer.astro';    // ここも「../../」
import { SITE_TITLE, SITE_DESCRIPTION } from '../../consts'; // ここも「../../」
import { getCollection } from 'astro:content';
import FormattedDate from '../../components/FormattedDate.astro'; // ここも「../../」

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
---
<!doctype html>
<html lang="ja">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<div style="padding: 2em 0; border-bottom: 1px solid #eee;">
				<h1>🚀 {SITE_TITLE}</h1>
				<p>投資とビデオブログの制作過程を記録しています。</p>
			</div>
			<section>
				<h2 style="margin-top: 1.5em;">最新の記事</h2>
				<ul style="list-style: none; padding: 0;">
					{posts.map((post) => (
						<li style="margin-bottom: 1em; display: flex; gap: 1em;">
							<FormattedDate date={post.data.pubDate} />
							<a href={`/blog/${post.slug}/`} style="font-weight: bold; color: var(--accent);">{post.data.title}</a>
						</li>
					))}
				</ul>
				<div style="text-align: right; margin-top: 2em;">
					<a href="/blog">→ すべての記事を見る</a>
				</div>
			</section>
		</main>
		<Footer />
	</body>
</html>

